# Type Content here...
# Input
a, b, c = input().split()

# Store digits in a list
digits = [a, b, c]

# Generate permutations manually
for i in range(3):
    for j in range(3):
        for k in range(3):
            if i != j and j != k and i != k:
                print(digits[i] + digits[j] + digits[k])v
