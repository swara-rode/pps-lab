# Type Content here
# Input
text = input()

# Initialize result string
result = ""

# Keep only letters, digits, and spaces
for char in text:
    if char.isalnum() or char.isspace():
        result += char

# Output
print(result)v
